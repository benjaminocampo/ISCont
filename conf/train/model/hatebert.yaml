module: ${eval:AutoModelForSequenceClassification.from_pretrained}
fit_module: ${eval:fit}
evaluate_module_dev: ${eval:evaluate}
evaluate_module_test: ${eval:evaluate}
data_module_train: ${eval:prepare_data}
data_module_dev: ${eval:prepare_data}
data_module_test: ${eval:prepare_data}
model_module_load_eval: ${eval:AutoModelForSequenceClassification}
params:
  model_name_or_path: GroNLP/hateBERT
  sample_size: null
